a day in review
On this day, we reviewed package management and started on process management. Process management is how a computer manages and controls all the tasks (or processes) that are running at any given time. These processes could be anything from the web browser you're using to the background tasks that keep your system running smoothly. The operating system decides which process runs and for how long so that everything stays smooth. Also, processes need memory, CPU time, and access to files, so the OS makes sure they don’t step on each other’s toes. The operating system also stops programs that are functioning properly and the different states that processes can be in
